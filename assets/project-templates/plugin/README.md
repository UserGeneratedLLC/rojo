# {project_name}
Generated by [Atlas](https://github.com/UserGeneratedLLC/rojo) {rojo_version}.

## Getting Started
To build this plugin to your local plugins folder, use:

```bash
atlas build -p "{project_name}.rbxm"
```

You can include the `watch` flag to re-build it on save:

```bash
atlas build -p "{project_name}.rbxm" --watch
```

For more help, check out [the Rojo documentation](https://rojo.space/docs).
